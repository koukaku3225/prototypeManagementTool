# 中間目標タブ

日付：2026-09-17
種別：機能追加
状態：実装・ローカル確認済み。本番反映は下記

## 結論

下タブに「中間目標」を足した（今日｜中間目標｜目標｜わたし）。
目標を今週・今月の**測れる目安**（時間／回数／達成）に分け、時間割の予定とつなげる。
期間が終わった中間目標は、結果を見ながら「続ける／目安を変える／終わりにする」を選ぶ。

- 設計：[docs/superpowers/specs/2026-09-17-checkpoint-tab-design.md](../superpowers/specs/2026-09-17-checkpoint-tab-design.md)
- モック：https://claude.ai/artifact/8vGERNoGBCShCjSBJKQnEd

## 背景

目標はあっても、今日やることが「何のためか」につながらず、ブレイクダウンが弱かった。
週・月の中間目標（Checkpoint）はすでにあったが、目標の詳細を開かないと見えず、
数値の目安も、時間割とのつながりも持っていなかった。

## 変更内容

| 場所 | 内容 |
|---|---|
| `types/goal.ts` | Checkpoint に `measure`（時間/回数/達成）・`target`・`manualCount`・`previousId` |
| `types/timebox.ts` | TimeBox に `checkpointId` |
| `lib/checkpoint.ts` | 進み具合 `checkpointProgress`、今の期間 `currentCheckpoints`、振り返り待ち `pendingReviews`、引き継ぎ `closeAndCarryOver`、予定シートの選択肢 `checkpointOptionsForBox`、`?checkpoint=` の読み取り |
| `app/checkpoints/page.tsx` | 新しいタブ。一覧・足すシート・振り返り |
| `components/TimeBoxSheet.tsx` | 「どの中間目標か」と、入れたときの見込み（例 5.5 → 6 / 8時間） |
| `app/plan/page.tsx` | `/plan?checkpoint=<id>` で紐づけ済みの新しい予定を開く |
| `components/BottomNav.tsx` | タブを追加 |
| `lib/supabase/mappers.ts` | 新しい列の送受信。`checkpoint_id` は UUID の形でなければ null |
| Supabase | `checkpoints.measure/target/manual_count/previous_id`、`timeboxes.checkpoint_id`（本番に適用済み。追加のみ） |

## 仕組み

- **時間**：紐づけた予定の合計（予定を入れた時点で数える）。完了した時間は内訳で出す
- **回数**：完了した紐づけ予定の数 ＋「＋1」で足した数
- **達成**：チェック（status を done に）
- 数えるのは、非表示でなく日付が期間内の予定だけ
- **振り返り**：達成していれば done、未達なら abandoned（既存の「今回は終わりにする」）で閉じる。
  続けるなら今の期間で新しく作る。今の期間に同じものが既にあれば作らず、「目安を変える」ならそちらの目安を変える

## 検証

| 確認 | 結果 |
|---|---|
| `npm test`（中間目標 43件・マッパー 12件ほか全件）・`tsc`・`next build` | 通過 |
| ローカル（スマホ幅）で一覧の表示 | ✅ 5.5 / 10時間（完了4.5）、先週 7.5 / 10時間 を表示 |
| 振り返り「目安を変えて続ける → 8」 | ✅ 先週は終わり、今週の目安が8に。二重に作られない |
| 回数の「＋1」・達成のチェック・足す（回数3回／今月） | ✅ |
| 「予定に入れる」→ 予定シート → 保存 | ✅ 目標と中間目標が入った状態で開き、保存後 6 / 8時間 |
| コンソールのエラー | なし |

途中で見つけて直したもの：
- チェックした「達成」が一覧から消えて取り消せなかった → できたものも一覧に残す
- 振り返りで、今週に同じ中間目標があると二重に作られた → 作らず、目安を変える場合はそちらを変える
- 過去に完了した予定があるのに「紐づけた予定はまだありません」と出た → 「この先の予定はまだありません」

## 動かして直すループ（2026-09-17、3周）

| 周 | 確認したこと | 見つけた不具合 → 対応 |
|---|---|---|
| 1 | 振り返り（週と月の混在・終わりにした同名あり）、回数、期間外の予定 | ①「二重に作らない」判定が終わりにした中間目標まで数え、選んでも新しく作られなかった → 数えない ②「＋1」を戻せない →「−1」 ③ 週と月が混ざると先頭の期間表示がどれの期間でもない → 1件ずつ期間を出す ④ 期間外の予定で見込みが「5.5 → 5.5」→ 数に入らないと伝える |
| 2 | 今日（リスト）画面からの導線、目標ごとの足す | ⑤ 今日の画面の中間目標が目標の詳細へ飛んでいた → 中間目標タブへ ⑥ 中間目標の無い目標から足すと先頭の目標が選ばれていた → その目標を選んで開く |
| 3 | 本番のクラウド保存（本人が作った実データ） | 新しい不具合なし。`measure`・`target`・予定の `checkpoint_id` がクラウドに入り、画面の 0.5 / 3時間 と一致 |

すべてローカル（スマホ幅）で実際に操作して確認し、`npm test`・`next build` 通過のうえ本番へ反映した（b940a21）。

## 残件

- 目標の詳細画面の中間目標エディタでは、測り方・目安をまだ編集できない（タブで足したものは表示・引き継ぎされる）
- 今日の画面の「いまの中間目標」に進み具合（数値）はまだ出していない
- 振り返りで目安を変えても、タイトル（例「週10時間」）は自動では変わらない
- 間違えて足した中間目標を、タブから消す・終わりにする手段が無い（目標の詳細か、期間が終わったあとの振り返りで閉じる）
