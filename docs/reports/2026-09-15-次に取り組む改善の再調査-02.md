# 次に取り組む改善の再調査（本日2回目）

日付：2026-09-15
種別：調査
状態：完了（コード変更なし）

## 結論

本日すでに実行済みの棚卸し（[docs/reports/2026-09-15-止まっていたタスクの棚卸しと修正.md](2026-09-15-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)、
専用カレンダー同期の400と開きっぱなし端末の削除を修正済み）に続けて、AGENTS.md・直近7日のコミット・
`残件.md`・未コミットファイルを読み直したうえで、コードを再度当たった。
**根拠のある新しい改善提案は見つからなかった。** 無理に3件をひねり出すことはせず、コード変更・コミットは行っていない。

## 依頼と背景

`daily-next-plan` スケジュールタスク。次に取り組む価値の高い改善を3件、優先度付きで提案し、
判断不要なものは実施してコミット・pushまで行う依頼。

## 変更内容・調査結果

以下を確認したが、いずれも「すでに`残件.md`に判断待ちとして載っている」か「今回のコードには問題が見当たらない」だった。

1. **AGENTS.md の既知の落とし穴（日付処理・localStorage直書き・API認証・signal伝播）を機械的に再走査。**
   `toISOString().slice(0,10)` は `date.ts` 自身と `mappers.ts` の日付バリデーションのみ（規約違反ではない）。
   `localStorage.setItem` の直呼びは `storage.ts` の `writeDeviceFlag()` 内だけ（既存の正規ルート）。
   全APIルートで `requireAuthIfEnabled()` と `signal: req.signal` の呼び出しを確認、漏れなし。
2. **本日未明に入った `gc.pushBase`（差分プッシュ）の実装（[sync.ts](../../src/lib/supabase/sync.ts) / [sync-decision.ts](../../src/lib/supabase/sync-decision.ts)）を読み直した。**
   書き込み成功後にだけ基準を進める順序、`resetAll()` が `DEVICE_KEY` を丸ごと消すため新キーの消し忘れが無いこと、
   別アカウント時に基準を無視するガードを確認。目立つ不具合は見当たらなかった
   （`残件.md` の R20「別端末で消したものが戻る」は、この基準を使えば直せると書かれているとおりで、
   すでに判断待ちとして載っている）。
3. **対話フェーズの上限（`PHASE_TURN_LIMIT` / `PHASE_TURN_MIN`）を読み直した。**
   各値には実機検証で見つかった不具合の経緯がコメントで残っており（R10 など）、
   直近で調整されたばかり。新しい不整合は見つからなかった。
4. **`TODO` / `FIXME` などのマーカーを `src/` 全体で検索したが0件。**
5. `残件.md` の判断待ち（R13、R20〜R25）は、いずれも竜一の判断が要る性質（データの残し方の方針、
   本番DBの権限変更、本人のGoogleカレンダーでの実機操作、削除してよいデータの確認）で、
   コード上の根拠だけでは決められない。今回新しい情報は加わっていないため、判断を仰ぐ内容として再掲するに留め、
   同じ提案を新規扱いで繰り返すことはしなかった。

## 仕組みの説明

該当なし（コード変更なし）。

## 関連ファイル・根拠

- [AGENTS.md](../../AGENTS.md) — 規約の再走査対象
- [残件.md](../../残件.md) — 既存の判断待ち・未検証一覧
- [src/lib/supabase/sync.ts](../../src/lib/supabase/sync.ts)、[sync-decision.ts](../../src/lib/supabase/sync-decision.ts) — 本日の差分プッシュ実装の読み直し
- [src/types/goal.ts](../../src/types/goal.ts)、[src/lib/prompts/phases.ts](../../src/lib/prompts/phases.ts) — 対話フェーズの上限の読み直し

## 確認したこと

- `npm test`：全スイート成功（回帰含む。ログに出るエラーはテストが意図的に発生させた異常系のもの）。
- `src/` 全体で `TODO`/`FIXME`/`XXX`・`localStorage.setItem` 直呼び・`toISOString().slice(0,10)` 誤用を検索、規約違反なし。
- 全APIルートで `requireAuthIfEnabled()` と `signal: req.signal` の有無を確認、漏れなし。
- GitHubリポジトリの公開範囲はネットワーク到達不可のため確認できなかった（R22 は引き続き未解決のまま）。

## 残っていること

- 新しい実装は行っていない。`残件.md` の判断待ち（特に R20〜R25、R13）は竜一の判断待ちのまま。
- 未コミットのレポート・スクリプト（`docs/reports/2026-09-14-*` ほか）は R22 のとおり、
  リポジトリの公開範囲が分からないため今回もコミットしていない。
- 次回も同じ観点で調査するより、**R20〜R25 のいずれかで竜一の判断が下りた時点**に実装へ進むほうが、
  この時点での再調査より価値が高いと考えられる。
