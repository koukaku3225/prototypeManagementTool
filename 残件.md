# 残件

判断が要る、または未検証で残っているもの。片づいたら消す。
新しく増えたらここに足す。

最終更新: 2026-09-19 その2（目標・対話ログの書き出しで「作成日」がUTCの日付になり、JST深夜〜早朝に作ったものは前日表示になっていたのを直した）

---

## 判断を待っているもの（竜一に決めてほしい）

### R28. 「達成」型の中間目標を「できた」にすると、次の期間へ引き継がれない（2026-09-19）

振り返り（期間が終わった中間目標）に出るのは**活動中のまま期間が終わったもの**だけ。
「できた」にチェックを付けたものは出ないので、毎週くり返したい種類のもの
（例「今週の出会いの予定を立てる」）は、**できた週ほど自分で足し直す**ことになる。
**案A：いまのまま（できた＝その回は終わり）／案B：できた達成型も振り返りに出し「もう一度やる／終わりにする」を選ばせる／
案C：中間目標に「毎週くり返す」印を付けられるようにして、印のあるものだけ自動で次の期間へ**

### R29. main に入ったままのブランチと作業ツリーを消してよいか（2026-09-19）

`feat/checkpoint-tab`・`feat/google-primary-import` は main にマージ済みだが、手元にも GitHub にも残っている。
作業ツリーも2つ残っている（`副業/product/pmt-primary-import`、`.codex/worktrees/81a2/prototypeManagementTool`）。
R3（2026-09-14）では同じものを竜一の判断で消した。消してよければ次回まとめて消す。

### R26. 中間目標の目安を変えたとき、タイトルの数字をどうするか（2026-09-18）

9/17 の中間目標タブの残件。例「週10時間」の目安を 8 に変えても（振り返り・目標の詳細のどちらでも）、
タイトルは「週10時間」のまま、表示は「x / 8時間」になる。タイトルは本人の言葉なので、自動では触っていない。
**案A：いまのまま／案B：タイトルに目安と違う数字があれば「タイトルも直しますか？」と聞く／案C：「数＋単位」だけ自動で置き換える**

### R27. リマインダー（通知）をどの段階から始めるか（2026-09-17 提案）

[提案](docs/reports/2026-09-17-reminder-options.md)（[HTML](docs/reports/2026-09-17-reminder-options.html)）。
段階1：専用カレンダーの予定に通知を付ける（半日）→ 段階2：ntfy で「明日の時間割が空」などを1週間試す →
段階3：Webプッシュ（PWA）か LINE Messaging API。着手するか、どこからかを決めてほしい。

### R20. 別の端末で消したものが、それを持っている端末を開くと戻ってくる（墓標）

2026-09-14 の合体（`mergeWithReport`）は「片方にしか無いものは両方残す」ので、
PCで消した予定を、まだそれを持っているスマホを開くとクラウドへ戻す。
2026-09-15 に端末ごとの「前回そろえた各行の指紋」（`gc.pushBase`）を入れたので、
**それを使えば「前回そろえた時にクラウドにあった・この端末で変えていない・いまクラウドに無い
＝別端末で消された」と判定して落とせる**（DBの変更は不要）。
ただし、クラウドのデータが事故で消えたとき（9/14 のスマホの件）にも同じ判定で端末側まで消えるので、
9/14 に「消えるより戻るほうが取り返しがつく」と倒した判断をひっくり返すことになる。
**案A：いまのまま（戻る）／案B：指紋で判定して落とす（一度に多数なら止めて聞く）／案C：DBに削除の記録テーブルを作る**

**2026-09-17 追記：実害の出る場面が具体的になった。** 9/16 夜に Google で繰り返し予定を消し、65件が「Googleで削除済み」のまま残っている。
9/17 に時間割へ「まとめて片付ける」を足したが、同じ65件を持ったままの別の端末があると、その端末を開いたときにクラウドへ戻り、
帯がまた出る（仕組み上そうなる。実機では未確認）。案B ならこの往復が止まる。

### R21. セキュリティレビュー指摘5・6（本番DBの権限）

[報告](docs/reports/2026-09-14-security-fixes.md)（未コミット）のとおり。
- 指摘6：Google の `refresh_token` を本人のセッションから直接 SELECT できる。service role 鍵を Vercel に置くか、列権限を剥がして SECURITY DEFINER 関数へ移すか
- 指摘5：外部AI（MCP）の OAuth クライアントを登録する**前に**、`client_id` を見る RLS が要る（いまは0件なので入口なし）
- 指摘2の残り：ログアウト後も同じブラウザに前の人のデータが見える（共有端末で使うか次第）

**2026-09-16 追記：GitHub のリポジトリ（koukaku3225/prototypeManagementTool）は公開されている。**
ログインせずに GitHub API で見て `private: false` だった。コードを誰でも読めるので、指摘6は優先度を上げて判断してほしい。

### R22. 未コミットのまま残っている資料をリポジトリに入れるか

`docs/reports/2026-09-14-security-review.md`（.html）・`security-fixes.md`・`security-review-evidence.cjs`・
`security-fix-verification.cjs`・`カレンダー同期の実機検証.md`・`カレンダー連携の鍵を本番へ.md`・
`calendar-routes.md`・`src-folder-structure.md`・`web-for-cpp-explainer-skill.md` と `.agents/skills/web-for-cpp-explainer/`。
セキュリティレビューは**未修正の指摘の再現手順**を含み、GitHub リポジトリの公開範囲をこちらから確認できなかったので、
2026-09-15 の定期実行ではコミットしていない。非公開なら全部入れてよいはず。

**2026-09-16：公開リポジトリだと分かった**（R21 の追記）。セキュリティレビューと検証スクリプトは、
公開のままならコミットしない。**案A：GitHub で非公開にしてから全部入れる／案B：再現手順を含む4件
（security-review.md/.html・2つの .cjs）以外だけ入れる／案C：手元に置いたまま**。今回も入れていない。

### R23. 退避データの片付け

- Supabase の `backup.*_20260914_1600`（9/14 のデータ消失の復旧前に退避したもの）
- この PC の Chrome の localStorage の `zz.20260914-1550-before-restore.*`（15キー）と `zz.backup.timeboxes.20260914-before-caltest`
確認が済んでいれば消してよい。取り返しがつかない削除なので、自動では消していない。

### R13. 完了済みの習慣枠を、別の日へドラッグ移動したとき

**いまの状態**
2026-09-11 その2 で「完了を取り消す／完了済みの習慣枠を削除する」ときに、
その枠が自動で付けた習慣の記録（`done`）も取り消すようにした
（[報告](docs/reports/2026-09-11-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6%E3%81%A8%E5%AE%8C%E4%BA%86%E5%8F%96%E3%82%8A%E6%B6%88%E3%81%97%E3%81%AE%E4%BF%AE%E6%AD%A3.md)）。

残っているのは、**完了済みの習慣枠を別の日へドラッグ移動**したケース。
記録は元の日に、枠は新しい日に残る（`moveByDrag` は日付をまたぐ移動もできる）。
稀で、直すと「移動先の日にも `done` を付け直すのか」「元の日の記録は消すのか」の
判断が要り、実装も煩雑。**現実にはほぼ起きない**（完了した枠をわざわざ別日へ運ぶ）ため、
様子見。起こす価値が出たら `saveBox` / `save` の日付変更検知として足す。

---

## 未検証のまま残っているもの

### R5. AI対話（`/story/new` → `/session`）の通し

`/api/chat` の課金が発生するため、自動検証では入口までしか踏んでいない。
`PHASE_INSTRUCTIONS` と `PHASE_TURN_LIMIT` のかみ合わせ（AGENTS.md に注意書きあり）は
実際に最後まで通さないと確認できない。

### R6. ログイン後のクラウド同期・カレンダー連携の本番往復

別端末で同じデータが見えるか、カレンダーの予定が本当に往復するか。
Googleアカウントへの権限付与とパスワードの入力を伴うので、代行できない。

**これに紐づく未確認が3つある。**

1. **設定画面の「読み取りの許可が足りません」ブロック**（2026-09-09 追加）。
   ログイン済みかつ連携済みでないと表示条件に入らないため、実画面に出せていない。
   2026-09-11 に理由を特定した。`CalendarLink` は**Supabaseにログインしていないと、
   カレンダーの状態を取りに行く手前で「先にログインしてください」に分岐する**ので、
   ログインを代行できない以上ここから先へ進めない。
   **時間割側の帯・端末の印・印が消えることまでは実機で確認済み**
   （応答を差し替えて3通りを通した。2026-09-11 の報告）
2. **`CalendarSyncBoot` の繋ぎ直し処理**。ログインしていないと
   `getSyncState()` が `off` のままで、そもそも走らない。
   ロジックは `tests/calendar-reconnect.test.mjs`（13件）で固定してある
3. **`google_calendar_links` のRLS**。9/14 のセキュリティレビューで読み取り確認済み（本人の行だけ全操作可）。
   ただし本人のセッションから `refresh_token` を読める点が R21 に残っている
4. **中間目標のクラウド同期（R16、2026-09-14）**。テーブルとRLSは本番DBで確認済み
   （本人の行は書け、他人の行は拒否。取り消すトランザクションで確認）。
   ログインした端末で中間目標がクラウドに上がり、別端末で見えるかは未確認
5. **繋ぎ直しの誤爆ガードと、同期の権限切れの案内（R12、2026-09-14）**。
   Google との実通信では未確認（R7 が済むまで試せない）。テストと応答の差し替えで確認済み

### R24. スマホで目標2件・今日の予定が見えるか（9/14 のデータ消失の復旧確認）

[報告](docs/reports/2026-09-14-%E3%82%B9%E3%83%9E%E3%83%9B%E3%81%A7%E7%9B%AE%E6%A8%99%E3%81%A8%E4%BA%88%E5%AE%9A%E3%81%8C%E5%87%BA%E3%81%AA%E3%81%84%E3%83%BB%E3%82%AF%E3%83%A9%E3%82%A6%E3%83%89%E3%81%AE%E3%83%87%E3%83%BC%E3%82%BF%E6%B6%88%E5%A4%B1.md)。本人の端末でしか確かめられない。

### R25. メインカレンダー取り込みの本番操作確認（9/14 の残り）

[報告](docs/reports/2026-09-14-%E3%83%A1%E3%82%A4%E3%83%B3%E3%82%AB%E3%83%AC%E3%83%B3%E3%83%80%E3%83%BC%E3%81%AE%E5%8F%96%E3%82%8A%E8%BE%BC%E3%81%BF.md)の残件1。
Google で予定を足す／時刻を変える／消す → 時間割を開き直して枠が追うか、アプリで消して戻らないか。
**本人の Google カレンダーに予定を作って消す操作**になるので、定期実行では行っていない。
（2026-09-15 に、専用カレンダーへの同期が 400 で止まっていた件は直した。そちらは本番で確認済み）

**2026-09-17 追記：足す・消すは本番DBに形跡があった。** 9/16 21:28〜21:54 に Google で足した予定4件が取り込まれ、
消した繰り返し予定の65件に削除済みの印が付いていた（ブレーキが効いて残った件は 9/17 に直した）。
残りは**時刻を変えたときに枠が追うか**と、**時間割の「まとめて片付ける」で65件が消えるか**。

---

## 直したもの（記録。消してよい）

### 2026-09-19 その2（作成日の表示がUTCで前日になる不具合を直す）

詳細は
[docs/reports/2026-09-19-作成日の表示がUTCで前日になる不具合を直す.md](docs/reports/2026-09-19-%E4%BD%9C%E6%88%90%E6%97%A5%E3%81%AE%E8%A1%A8%E7%A4%BA%E3%81%8CUTC%E3%81%A7%E5%89%8D%E6%97%A5%E3%81%AB%E3%81%AA%E3%82%8B%E4%B8%8D%E5%85%B7%E5%90%88%E3%82%92%E7%9B%B4%E3%81%99.md)。

- **目標の書き出し（Markdown/JSON）と対話ログの書き出しで、「作成日」やファイル名の日付が
  JST朝9時までは前日になっていた**のを直した（`card.createdAt`/`session.startedAt` を
  `.slice(0, 10)` で切っていた3ファイル・4箇所を `toLocalDate(new Date(...))` に置き換え）。
  `AGENTS.md`「日付」節が名指しで禁止しているパターンが、書き出し（export）まわりだけ
  残っていた。
- 回帰テスト2件（`tests/export.test.mjs` を新規追加）。`npm test`（684件）/
  `npx tsc --noEmit` / `npm run build` 通過。**ブラウザでの実機確認は未実施**
  （定期実行環境は開発サーバーを起動できない。書き出しはファイル保存を伴うため
  コードの読み合わせとユニットテストで確認）
- 新しい判断待ちは見つからなかった

### 2026-09-19（止まっていたタスクの棚卸しと修正）

詳細は
[docs/reports/2026-09-19-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-19-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。

- **Google カレンダーで消された予定（「Googleで削除済み」と薄く残る枠）が、時間の合計に数え続けていた**のを直した（8ed313e）。
  時間割の「予定○時間・○件」・目標の「今週／累計」・中間目標の「○/○時間」すべてに入っていて、
  **「まとめて片付ける」を押した瞬間に数が黙って減る**状態だった（本番データには今週ぶんだけで6件・各1時間）。
  完了にしてあるものは実際にやった記録なので残す（`countsAsPlanned`）
- **時間・回数の中間目標を目標の詳細で「完了にする」と、中間目標タブでは見た目が変わらないのに今日の画面から消え、
  「＋予定に入れる」を押しても何も起きなくなっていた**のを直した。タブに「できた」を出し、「⋯」から取り消せるようにした
- 回帰テスト8件（timebox 4・checkpoint 4）。**修正前は4件が落ちる**。`npm test`（682件）/ `npx tsc --noEmit` / `npm run build` 通過
- ローカルと本番URLの両方で操作して確認。本番DBは読み取りのみ（作業前後で変化なし）
- 判断が要るものとして R28（達成型の引き継ぎ）と R29（ブランチの片付け）を足した


### 2026-09-18（止まっていたタスクの棚卸しと修正）

詳細は
[docs/reports/2026-09-18-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-18-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。

- 9/17 の中間目標タブの残件のうち3件を実装（cbf704f）：目標の詳細で測り方・目安を編集、今日の画面に進み具合（「5.5 / 10時間」）、タブの行の「⋯」から終わりにする・消す
- **振り返りで「目安を変えて続ける」の目安を空欄・0 にしても始められ、黙って前と同じ目安で続いていた**のを直した
- ローカルと本番URLで操作して確認。ログインした端末でクラウドに届くかは未確認（既存の保存経路を使用）
- 残件のうちタイトルの数字（R26）は判断待ちへ

### 2026-09-17（止まっていたタスクの棚卸しと修正）

詳細は
[docs/reports/2026-09-17-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-17-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。

- **Google で繰り返し予定を消すと、取り込んだ枠が「Googleで削除済み」のまま時間割に残り続けていた**（本番で65件）。
  一度に6件以上はブレーキで消さない作りで、次の同期でもブレーキがかかり続けるのに、画面には1件ずつ消す手段しか無かった。
  時間割に件数の帯と「まとめて片付ける」（確認つき・取り消しあり）を足した。書き込み・完了のある枠は対象外（9aabf5b）。
  本番URLで帯・片付け・取り消しを確認。**竜一さんの65件は本人のデータなので消していない**
- 削除済みの枠の説明文と消す確認文が事実と違っていたのを直した

### 2026-09-16（止まっていたタスクの棚卸し）

詳細は
[docs/reports/2026-09-16-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-16-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。

- **コード変更なし。** 自分の判断で直せる未完了タスクは残っておらず、新しい不具合も見つからなかった
- 本番DBの同期状態（`last_error` なし）、`npm test`・`npx tsc --noEmit`、ブラウザでの主要画面の操作（目標・習慣・完了と取り消し・中間目標・記録・削除の連鎖）を確認
- GitHub リポジトリが公開と分かった（R21・R22 に追記）
- カレンダー同期の AbortSignal 伝播は、途中で止めると作った予定のIDを返せなくなるため見送り

### 2026-09-15（止まっていたタスクの棚卸しと修正）

詳細は
[docs/reports/2026-09-15-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-15-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。

- **専用カレンダーへの同期が、9/14 の一方向化（943c81b）から毎回 400 で止まっていた。**
  画面が送らなくなった `hasNotes` を API のスキーマが必須のまま持っていた。任意にし、本文の組み立てを
  `calendar/payload.ts` に出してスキーマとのかみ合わせをテストで固定（d411427）。本番で 200・予定IDの付け直しを確認
- **開きっぱなしの端末で1件保存すると、別端末が足した行をクラウドから消し、直した行を古い中身で上書きしていた。**
  本番で再現してから、端末ごとの基準（`gc.pushBase`）で「変わった行だけ書き、この端末で消した行だけ消す」にした（0ce59f0）。
  本番で同じ手順を通し、別端末の追加・修正が残ること、この端末での削除は届くことを確認
- **R7 を閉じた。** 本番に鍵が入り、連携・同期・メインカレンダー取り込みまで動いている（9/14 の報告）
- 9/14 の実機検証の不具合A・B・C は、943c81b の一方向化で経路が無くなっていることをコードで確認（C は判断表で「消された → 作り直す」）

### 2026-09-14 その6（R12・R16 の実装、R17・R18 を閉じた）

詳細は
[docs/reports/2026-09-14-中間目標のクラウド同期と繋ぎ直しのガード.md](docs/reports/2026-09-14-%E4%B8%AD%E9%96%93%E7%9B%AE%E6%A8%99%E3%81%AE%E3%82%AF%E3%83%A9%E3%82%A6%E3%83%89%E5%90%8C%E6%9C%9F%E3%81%A8%E7%B9%8B%E3%81%8E%E7%9B%B4%E3%81%97%E3%81%AE%E3%82%AC%E3%83%BC%E3%83%89.md)。

- **R16（案B）**：Supabase に `checkpoints` テーブルを追加し、中間目標と建て方の評価を同期するようにした。
  端末ごとに最初の1回だけクラウドとローカルを合わせてから送る（別端末の中間目標を消さないため）
- **R12（①②とも）**：IDが変わっても連携を作り直した時刻が新しくなければ、枠のIDを落とさず同期を見送る。
  予定の書き込みで権限切れが起きたときも「連携し直して」を出す（混雑には出さない）
- **R17**：竜一の判断で、いまのまま（整理の待ち時間は縮めない）
- **R18**：竜一の判断で、対応不要（R10 で入れた手当てのまま）


### 2026-09-14 その5（R19：今日の画面に中間目標を出す）

詳細は
[docs/reports/2026-09-14-今日の画面に中間目標を出す.md](docs/reports/2026-09-14-%E4%BB%8A%E6%97%A5%E3%81%AE%E7%94%BB%E9%9D%A2%E3%81%AB%E4%B8%AD%E9%96%93%E7%9B%AE%E6%A8%99%E3%82%92%E5%87%BA%E3%81%99.md)。

- 竜一の選択で案A（今日の画面の先頭）だけ。予定シート（案B）とわたし画面（案C）は作っていない
- 期間が今日を含む活動中の中間目標を最大2件。期間が過ぎて閉じていないものは「振り返る」として先頭。残りは「ほかN件」


### 2026-09-14 その4（R10・R11 の実装）

詳細は
[docs/reports/2026-09-14-対話の上限と逃げ道・書き出し案内.md](docs/reports/2026-09-14-%E5%AF%BE%E8%A9%B1%E3%81%AE%E4%B8%8A%E9%99%90%E3%81%A8%E9%80%83%E3%81%92%E9%81%93%E3%83%BB%E6%9B%B8%E3%81%8D%E5%87%BA%E3%81%97%E6%A1%88%E5%86%85.md)。

- **R10**：`woop_wbs` の上限を 7→10。対話画面に「別の質問にする」（ターンに数えない）と
  「切り上げて次へ／ここで終える（非推奨・確認つき）」を追加。
  共通の原則に「同じ型の問いを続けない・根っこの欲求に着いたら掘り続けない・『もういい』で先へ進める」を追加（R18 の一部もこれで手当て）
- **R11**：未ログインで中身があるとき、わたし画面に「この端末にしか残っていない」案内と「いま書き出す」


### 2026-09-14 その3（判断3件の反映）

詳細は
[docs/reports/2026-09-14-判断3件の反映.md](docs/reports/2026-09-14-%E5%88%A4%E6%96%AD3%E4%BB%B6%E3%81%AE%E5%8F%8D%E6%98%A0.md)。

- **R1（短い呼び名を一覧でも使う）は「いらない」で見送り。** コードは変えていない
- **R2' を案Cで実装した。** 作成日は「できた／最小版」の記録があるときだけ、
  連続日数と達成率に数える。記録が無い・休み・できなかった作成日は、分母にも途切れにも入れない
- **R3 のマージ済みブランチ7本を削除した**（GitHub 側・手元側とも。削除前に全部 main に入っていることを確認）


### 2026-09-14 その2（取り消しの記録欠落と整理待ちの改善）

詳細は
[docs/reports/2026-09-14-取り消しの記録欠落と整理待ちの改善.md](docs/reports/2026-09-14-%E5%8F%96%E3%82%8A%E6%B6%88%E3%81%97%E3%81%AE%E8%A8%98%E9%8C%B2%E6%AC%A0%E8%90%BD%E3%81%A8%E6%95%B4%E7%90%86%E5%BE%85%E3%81%A1%E3%81%AE%E6%94%B9%E5%96%84.md)。

- **時間割で完了済みの習慣枠を「消す」→「取り消す」と、枠は戻るのに
  習慣の記録（連続日数・達成率）だけ戻らない不具合を直した。**
  `deleteTimeBox()` は完了済み習慣枠を消すとき記録も消すが、`/plan` の
  取り消し（`revert`）は `upsertTimeBox()` で枠だけ戻していて対称になっていなかった。
  `undoDeleteTimeBox()` を足し、記録も戻すようにした
- **整理（`/api/structure`、実測75秒）の待ち画面が、75秒間ずっと同じ文言のままだった
  のを直した。** 経過時間で文言を変える `structuringMessage()` を足し、
  `/big`・`/card` で重複していた待ち画面を `StructuringWait` に共通化した
- 回帰テスト8件（`storage` 4件・新規 `structuring-wait` 4件）。`npm test`
  （合計483件）/ `npx tsc --noEmit` / `npm run build` 通過。**開発サーバーは
  自動実行環境のため起動できず、ブラウザでの実機確認は未実施**（コードの
  読み合わせで経路を確認）
- 中間目標が日々の画面から見えない件はR19として残した（判断待ち）

### 2026-09-14（止まっていたタスクの棚卸しと修正）

詳細は
[docs/reports/2026-09-14-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-14-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。

- **クラウドから取り込むと、中間目標（と建て方の評価）が黙って全部消えていた。**
  `pullAll()` → `restoreState()` が対象キーを一度すべて消すのに、クラウドに
  テーブルの無い `gc.checkpoints` を持ち越していなかった。`carryOverOnPull()` に切り出して直した
- **R14 を直した。** 予定を1件消した直後に閉じても、削除がクラウドに届く。
  カレンダー同期の一括削除は `deleteTimeBoxes()` で全件送信を1回にした
- **R15 を閉じた。** 2026-09-14 未明のコミット（dc24b85）で、打ち切り・4xx は自動で再試行しなくなっていた
- 中間目標・習慣・つまずきが2件以上あると、「編集」「消す」などのボタンが同じ名前で並んでいたのを直した
- 2026-09-13 その2 の「習慣枠をドラッグ→取り消し」をブラウザで実際に操作して確認した
- 未 push だった dc24b85（整理の打ち切り修正）を今回の修正と一緒に main へ push した


### 2026-09-13 その2（次に取り組む改善3件）

詳細は
[docs/reports/2026-09-13-次に取り組む改善3件.md](docs/reports/2026-09-13-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6.md)。

- **対話が無警告で跡形もなく消える経路を3つ直した。** `/goal/new` と
  `/story/new` の「対話で決める」は、進行中の未完了対話があっても
  確認なしで `clearSession()` していた。`/history/[id]` の「続きから話す」は
  「そちらは記録に残したうえで置き換わります」と案内しながら、実際には
  退避せずに上書きしていた（文言と挙動の不一致）。共通の
  `archiveIfAbandoned()`（`src/lib/storage.ts`）を足し、3箇所とも
  上書き・削除の直前に未完了セッションを `gc.archive` へ退避してから進むようにした。
  UIの確認ダイアログは追加していない（挟むかどうかはUXの判断のため）。
  退避さえされていれば `/history` から後から見つけられる。
- **習慣枠をドラッグで動かして取り消すと、消せない幽霊レコードが残る不具合を直した。**
  `src/app/plan/page.tsx` の `moveByDrag` は、習慣から自動配置しただけの枠
  （id が `habit-` で始まる ghost）をドラッグで動かすと実体化して保存するが、
  取り消し（Undo）は動かす前の ghost オブジェクトをそのまま `upsertTimeBox()`
  していたため、`habit-` で始まる id を持つ**実体**が新規に保存されていた。
  `isGhost()` は id のプレフィックスで判定するため、この行は以後ずっと
  ghost 扱いのまま残り、`TimeBoxSheet` の削除ボタンが出ず（`fromHabit` 判定）、
  Google カレンダー同期の対象にもならない（`isGhostId` 判定）——UIから
  消せない重複行になっていた。取り消し時に ghost だったかどうかで分岐し、
  ghost だった場合は動かしてできた実体を `deleteTimeBox()` するようにした。
- **`/api/structure` に `signal: req.signal` が渡っていなかったのを直した。**
  `AGENTS.md` の規約（上流の生成はクライアント切断で止める）に反しており、
  `/api/chat` は対応済みだったが `/api/structure` は漏れていた。特に `/big`
  （最大16000トークン）は、タブを閉じても最後まで課金され続けていた。
  `client.messages.parse()` の第二引数に `{ signal: req.signal }` を渡すだけの修正。
- 回帰テスト5件（`tests/storage.test.mjs`、45 → 50件）。`npm test` /
  `npx tsc --noEmit` / `npm run build` 通過。**開発サーバーは自動実行環境のため
  起動できず、ブラウザでの実機確認は未実施**（コードの読み合わせで経路を確認）。

### 2026-09-13（目標削除の直後に閉じるとクラウドに孤児の枠が残る）

詳細は
[docs/reports/2026-09-13-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-13-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。
2026-09-11 のコードレビュー指摘1が、報告だけで修正が入っていなかったもの。

- ログイン中に目標を消し、**その1.5秒以内にタブを閉じる**と、クラウドに
  `card_id` が null の予定枠が残り、別端末で取り込むと
  「目標ごと消したはずの予定」が時間割に戻ってきていた。
  目標の削除（`gc.cards`）はすぐ届くのに、枠の削除（`gc.timeboxes`）は
  まとめ待ちに乗っていたため。クラウド側の外部キーは `timeboxes.card_id` を
  SET NULL にするだけで行を残す
- `setSyncFlushHook()` を足し、`deleteCard()` の末尾で待ちを解くようにした。
  消す操作は連打されないので、まとめる利点がそもそも無い
- 回帰テスト4件（`storage` 3件・`push-queue` 1件）。
  **修正前のコードでは3件が落ちること**を確認済み
- `npm test` / `npx tsc --noEmit` / `npm run build` 通過。
  ブラウザで目標の削除・その前後の画面を実際に操作して確認（コンソールエラー0件）。
  **クラウドへの実通信は未実施**（R6 と同じ理由）

**あわせて、2026-09-11 その2 の修正をブラウザで実際に操作して確認した。**
時刻付き習慣の枠を完了 → 取り消し → 再完了 → 削除まで通し、
「できた」の記録が付く／取り消される／枠と一緒に取り消されるところまで意図どおりだった。
前回「ブラウザ操作は未実施」と書いていたが、これで埋まった。

### 2026-09-11 その2（完了取り消しで習慣の記録が残る）

詳細は
[docs/reports/2026-09-11-次に取り組む改善3件と完了取り消しの修正.md](docs/reports/2026-09-11-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6%E3%81%A8%E5%AE%8C%E4%BA%86%E5%8F%96%E3%82%8A%E6%B6%88%E3%81%97%E3%81%AE%E4%BF%AE%E6%AD%A3.md)。

- 時刻を決めた習慣の枠を**完了 → 取り消し**しても、その日の「できた」記録が
  残り続け、連続日数と達成率が1日ぶん水増しされていた。時刻付き習慣は
  「今日の習慣」チェックリストに出ないので、後から取り消す手立ても無かった。
- 完了済みの習慣枠を**削除**したときも同じ（枠は消えるが記録は残る）。
- `clearHabitLogFromBox()` を追加し、`saveBox` / `save` / `deleteTimeBox` から
  「その枠がさっき付けた `done`（時刻が一致）」だけを取り消すようにした。
  手で付けた記録・`partial` / `skipped` / `missed` には触れない。
- 集計側（`habit.ts`）は無変更。記録が消えれば自動で「今日はまだ」に戻る。
- 回帰テスト7件（`tests/storage.test.mjs`、35 → 42 件）。`npm test` /
  `npx tsc --noEmit` / `npm run build` 通過。ブラウザ操作は 2026-09-13 に実施済み（下の 2026-09-13 の項を参照）。

### 2026-09-11（止まっていたタスクの棚卸しと修正）

詳細は
[docs/reports/2026-09-11-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-11-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。
どちらも 2026-09-10 のコードレビューが指摘したまま、修正が入っていなかったもの。

- **レビュー指摘1を直した。** 混んでいるだけの 403（`rateLimitExceeded` ほか）を
  「連携し直せば直る」と誤判定して、**再連携しても消えない案内**を端末に
  焼き付けていたのをやめた。逆に、再連携がいちばん要る `invalid_grant` は
  HTTP 400 で来るのでステータスだけでは拾えていなかった。
  `GoogleApiError` に本文の理由（`reason`）を持たせ、判定をそれに基づかせた
- **レビュー指摘2を直した。** 繋ぎ直しの後始末で、`localStorage` の容量超過に
  よって落とし切れなかった枠があっても「片付いた」目印を書いていた。
  目印が新しくなると二度とやり直されず、残った古いIDが処理窓に入った日に
  枠が黙って消える。**1件でも保存に失敗したら目印を書かない**ようにして、
  次回やり直せるようにした（`upsertTimeBox()` が保存の成否を返すようにした）

回帰テスト10件を追加し、**修正前のコードでは9件が落ちること**を確認済み。
ブラウザで実際に操作して、時間割の案内・枠の編集と保存・保存失敗の帯・
一覧／目標／わたしの各画面まで確かめた（コンソールエラー0件）。
**Googleへの実通信はしていない**（R6・R7 と同じ理由）。

### 2026-09-10（止まっていたタスクの修正）

詳細は
[docs/reports/2026-09-10-止まっていたタスクの修正.md](docs/reports/2026-09-10-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E4%BF%AE%E6%AD%A3.md)。

- **R8 を直した。** 1文字打つたびに時間割（と習慣）の全行をクラウドへ送っていたのを、
  打ち終わりを待って1回だけにした（`push-queue.ts`、キー別に1.5秒の末尾デバウンス）。
  localStorage への保存は今までどおり即座。閉じる・隠す瞬間に送り切る（`SyncBoot`）。
  ログアウト時と取り込み時は、待っているぶんを送らずに捨てる
- **R9 を直した。** 習慣の「どこでやるか」が、時間割の枠のメタ認知欄（対策）に
  `場所: …` として載るようにした（`habitBoxMeta()`）。実体化しても残る。
  場所の無い習慣の枠は今までどおり空
- 目標を消すときの確認文が、「やめた」習慣とその記録を数えていなかったのを直した
  （2026-09-09 の前日レビュー指摘1。報告だけで修正が入っていなかった）。
  `deleteImpactText()` に切り出し、やめた習慣の件数も言うようにした

ブラウザで実際に操作して確認済み（習慣の場所の表示・実体化・削除の確認文と実際の削除・
1文字ごとの localStorage 保存・空状態を含む周辺画面。コンソールエラーなし）。
**ログイン中のクラウド同期そのものは実通信で見ていない**（R6 と同じ理由）。

### 2026-09-09（その2：次に取り組む改善の調査）

詳細は
[docs/reports/2026-09-09-次に取り組む改善3件.md](docs/reports/2026-09-09-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6.md)。

- 対話で決めた「どこでやるか」が、明日の一歩の枠から消えていた
  （`firstStepMeta` に切り出し、If-Then と場所を両立させた。`goal-card.ts` / `card/page.tsx`）
- 過去の提案3件（不正UUID・初回オンボーディング・振り返りの再表示）は
  すべて実装済みであることを確認。再提案しない
- 判断待ちを2件足した（上の R8 / R9）

### 2026-09-09

詳細は
[docs/reports/2026-09-09-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-09-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。

- 繋ぎ直しのあと、時間割の枠が時間差で黙って消える（古いIDを localStorage 本体から落とす）
- 一度も同期に成功していない端末で、繋ぎ直しを永久に検知できない
- 重ね表示の403（権限不足）が握りつぶされて、理由がどこにも出ない
- `refresh_token` の重みが変わったのに、`link.ts` / `google.ts` の前提が古いまま
- `/api/calendar/overlay` だけ zod を通していない
- 習慣の作成日をUTC文字列から切り出していて、作った時刻で日数がぶれる
- 日本語として言わない「はじめて0日目」

**R4（ドラッグの実機確認）は今回ブラウザで実際に操作して確認したので、この項目は閉じた。**
移動・上端／下端のリサイズ・長押しの成立と不成立・つまみ・引いて作る、
マウスと指の両方で意図どおりだった。
ただしブラウザ自身のヒットテストとジェスチャ調停は経由していないので、
**指で1回だけ触ってもらえると完全に埋まる**。

### 2026-09-08

詳細は
[docs/reports/2026-09-08-目標と時間割のつながりを直す.md](docs/reports/2026-09-08-%E7%9B%AE%E6%A8%99%E3%81%A8%E6%99%82%E9%96%93%E5%89%B2%E3%81%AE%E3%81%A4%E3%81%AA%E3%81%8C%E3%82%8A%E3%82%92%E7%9B%B4%E3%81%99.md)。

- 目標を消すときに、巻き添えになる予定・習慣の件数を出す
- 完了した目標の習慣を、時間割と今日のチェックリストから外す
- 「手入力でつくる」「＋ 繰り返すことを足す」で、押しただけでは保存しない
- 予定シート・打刻バーで、完了した目標に紐づいた予定が「（紐づけない）」に見える食い違い
- 目標から「時間割で予定を作る →」に、目標を引き継ぐ
- 今日のリストが時刻順に並ばない
- ログインしていないとGoogleカレンダー連携ボタンが押せてしまう
- 表記のズレ（`何をを編集` / `M1〜M7`）
