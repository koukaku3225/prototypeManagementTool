# 残件

判断が要る、または未検証で残っているもの。片づいたら消す。
新しく増えたらここに足す。

最終更新: 2026-09-13（目標削除の直後に閉じるとクラウドに孤児の枠が残る不具合を修正、R14 追加）

---

## 判断を待っているもの（竜一に決めてほしい）

### R1. 「短い呼び名」を、目標一覧やリストでも使うか

**いまの状態**
`label`（短い呼び名）が効くのは、予定シートと打刻バーの目標プルダウンだけ。
`/goals`・`/list`・`/me`・`/card`・`WeekShareBar` は `vision.refined` の長文を
そのまま出している（`src/app/goals/page.tsx:387` ほか）。

入力欄の説明は「時間割で『どの目標のためか』を選ぶときに、ここで付けた名前が
短く表示されます」なので、**動作は説明どおり**である。
2026-09-09 に実際の画面で確かめ直した。呼び名「副業」を付けた状態で、
プルダウンは「副業」、`/goals` と `/list` は「半年後、副業で月5万円を…」の全文。

**なぜ勝手に直さなかったか**
`goalCardLabel()` は 14文字で切り詰める。これを `/goals` にそのまま当てると、
目標一覧が「半年後、副業で月5万円を安…」になる。
**一覧は全文が読めることに意味がある画面**なので、どちらが正しいかは
「呼び名は何のための名前か」という設計判断であり、実装で決めるべきではない。

**選択肢**

| 案 | 内容 | 影響 |
|---|---|---|
| A | いまのまま（プルダウン専用の呼び名） | 変更なし。説明文だけ「時間割の選択肢で使われます」と限定を強める |
| B | 呼び名があるときだけ、一覧でも呼び名を見出しにして全文を副文に添える | `/goals` `/list` `/me` の3画面。切り詰めは起きない |
| C | 全画面で `goalCardLabel()` に統一 | 一覧で全文が読めなくなる。非推奨 |

Bを推す。決まれば実装する。

---

### R2'. 習慣を作った当日の「できた」を、連続日数に数えるか

**もとのR2から変わった点**

もとは「`/me` に『はじめて0日目』と出て、押した手応えが返らない」という話だった。
2026-09-09 に調べたところ、**症状の半分は設計ではなく日付のバグだった**ので、
そこは直した（[報告](docs/reports/2026-09-09-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)）。

- `createdAt`（UTC文字列）を切り出して日付に使っていたため、
  **JSTの朝9時前に作った習慣だけ「作成日」が前日になっていた**
- そのせいで「0日目」と「1日目」が作った時刻でぶれ、
  `computeStreak` / `computeRate` のコメントにある「作成日そのものも数えない」も
  早朝に作った習慣だけ破られていた
- `habitStartDate()` を通すようにして、全時間帯で揃えた
- 日本語として言わない「はじめて0日目」は、当日だけ
  「今日つくったばかりです。」に変えた

**残っている判断**

揃った結果、**作った当日に「できた」を押しても連続日数は0のまま**になる。
`/list` の「今日の習慣 1/1」には出るので押した記録は見えるが、
`/me` の連続日数と「いまのところN日続いています」は出ない。

「作成日は数えない（その日はもう終わりかけ）」は既存コードの明示的な意図なので、
そのままにしてある。**朝に作ってその日にやった人にも手応えを返すなら、
この前提を変える話**になる。

| 案 | 内容 | 影響 |
|---|---|---|
| A | いまのまま（作成日は数えない） | 変更なし。夜に作った習慣を当日「できなかった」と数えない利点は保たれる |
| B | 作成日も予定日として数える | `computeStreak` / `computeRate` の `date <= start` を `< start` に。夜に作った習慣がその日いきなり「達成率0%」に寄与しうる |
| C | 「できた」の記録がある日だけ、作成日も数える | 分岐が増えるが、Bの副作用は出ない |

14日の待ち時間（`WARMUP_DAYS`）そのものは、いずれの案でも触らない。

**2026-09-10 追記：推奨は案C。**
[docs/reports/2026-09-10-次に取り組む改善3件.md](docs/reports/2026-09-10-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6.md)（改善3）で詰めた。
`date <= start` の break を `date < start` にし、`date === start` の日は
「`done`／`partial` の記録があるときだけ数え、無ければ分母にも入れない」。
案B（作成日を無条件に予定日扱い）と違い、夜に作ってやらなかった習慣が
その日いきなり達成率0%に落ちる副作用が出ない。決まれば実装する。

---

### R3. マージ済みブランチ7本が remote に残っている

`git branch --no-merged main` は空。つまり以下はすべて `main` に入っている。

```
feat/goal-short-label  feat/google-calendar-sync  feat/google-login
fix/cloud-sync-direction  fix/sync-safety  fix/timebox-sheet-complete-button
claude/claude-code-web-app-launch-53rwej
```

消してよいなら消す。残す理由があるなら残す。**勝手には消していない。**

---

### R10. woop_wbs のターン上限が、問い直し1回で「いつ・どこで」を取り切れない

**いまの状態**
`PHASE_TURN_LIMIT.woop_wbs = 7`（`src/types/goal.ts:31-43`）。
`woop_wbs` は「障害 → 状況 → If-Then → タスク選び → いつ・どこで」の5手で、
口火のアシスタント発言（ユーザー未回答で1ターン消費）を足すと、
**問い直しゼロ・時刻も場所も一発で確定したときだけ 7 にちょうど収まる**。

指示文（`src/lib/prompts/phases.ts:161-193`）は問い直しを前提にしている
（If-Then に最大2回、「『夜』は何時ごろ」「『家で』なら家のどこ」）。
1回でも問い直すと、「どこで」を答え切る前に `resolvePhase` が
`forced: true` で打ち切り、対話の唯一の持ち帰り＝明日の一歩が
時刻または場所の欠けた状態で `/card` の枠になる。

**提案（改善1）**
`woop_wbs: 7 → 10`（コメント更新）＋ `tests/phase-machine.test.mjs` に上限固定の回帰テスト。
small フローの最大は 16 → 19 ターン（廃止した5フェーズ39ターンの半分）。

**なぜ勝手に入れなかったか**
上限を上げると対話が長くなり、`mvp-spec` の「離脱の最大要因は『思ったより長い』」と
綱引きになる。値はUXの判断。効果の確認も対話1本＝課金（R5）が要る。

詳細：[docs/reports/2026-09-10-次に取り組む改善3件.md](docs/reports/2026-09-10-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6.md) 改善1。

---

### R11. 未ログイン利用者に「この端末にしか残っていない」ことが届いていない

**いまの状態**
`LocalBackupBoot` のディスクバックアップは `npm run dev` 専用（`AGENTS.md` 明記）。
**デプロイ環境で未ログインの間、自動の安全網はゼロ**。
書き出しは「設定」画面の下部にしかなく、`/me` から直リンクも無い（4手先）。
`AGENTS.md` は localStorage が丸ごと消えた実データ喪失の実例を記録している。

**提案（改善2・案A）**
`/me` のログインCTA直下に、未ログイン かつ 中身があるときだけ
「データはこの端末にしか残りません。ときどき書き出して保管を」＋「いま書き出す」ボタン。
`captureState()` / `download()` は既存なので表示分岐だけ。

**なぜ勝手に入れなかったか**
画面に要素を1つ足す。出す条件・頻度は設計判断。

詳細：[docs/reports/2026-09-10-次に取り組む改善3件.md](docs/reports/2026-09-10-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6.md) 改善2。

---

### R12. 繋ぎ直しの誤爆ガードと、再連携の案内をどこまで広げるか

**いまの状態**
`applyCalendarReconnect()` は「`/api/calendar/status` の `calendarId` が
前回と違う」だけで、**localStorage 全期間の枠の `googleEventId` を落とす**。
正規の繋ぎ直しでは正しいが、status が想定外の値を返したときの代償が大きい
（落ちた瞬間に目印も新カレンダーへ移り、次回は `changed=false` で自己回復しない。
送信窓内の枠は Google 側に重複予定として作り直され、窓外の枠はIDを失ったまま）。

2026-09-11 に**部分失敗のほうは直した**（1件でも保存に失敗したら目印を書かない。
[報告](docs/reports/2026-09-11-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)）。
残っているのは**誤爆そのものへのガード**で、こちらは設計判断。

| 案 | 内容 | 影響 |
|---|---|---|
| A | いまのまま | `link.calendarId` は再連携以外で変わらない想定なので、現実の誤爆確率は低い |
| B | 落とす範囲を送信窓（-14〜+90日）に限り、窓外は次回送信時に個別判定 | 誤爆の代償は減るが、2026-09-08 指摘1で塞いだ「窓外に古いIDが残る」穴が別の形で戻る |
| C | 目印に「いつ・どのユーザーとして」も持たせ、status の値が不審なときは落とさない | 判定材料は増えるが、状態が増える |

**あわせて判断が要ること。**
書き込み側（`insertEvent` / `patchEvent` / `deleteEvent` / `createCalendar`）は
`GoogleApiError` ではなく素の `Error` を投げる。つまり**同期の失敗は、どれだけ
権限が足りなくても「連携し直してください」の案内に繋がらない**（重ね表示の
経路だけが案内を出す）。広げると案内の出る場面が増えるので、出すかどうかは判断。

もとの指摘：[docs/reports/2026-09-10-前日分コードレビュー.md](docs/reports/2026-09-10-%E5%89%8D%E6%97%A5%E5%88%86%E3%82%B3%E3%83%BC%E3%83%89%E3%83%AC%E3%83%93%E3%83%A5%E3%83%BC.md) 指摘2。

---

### R13. 完了済みの習慣枠を、別の日へドラッグ移動したとき

**いまの状態**
2026-09-11 その2 で「完了を取り消す／完了済みの習慣枠を削除する」ときに、
その枠が自動で付けた習慣の記録（`done`）も取り消すようにした
（[報告](docs/reports/2026-09-11-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6%E3%81%A8%E5%AE%8C%E4%BA%86%E5%8F%96%E3%82%8A%E6%B6%88%E3%81%97%E3%81%AE%E4%BF%AE%E6%AD%A3.md)）。

残っているのは、**完了済みの習慣枠を別の日へドラッグ移動**したケース。
記録は元の日に、枠は新しい日に残る（`moveByDrag` は日付をまたぐ移動もできる）。
稀で、直すと「移動先の日にも `done` を付け直すのか」「元の日の記録は消すのか」の
判断が要り、実装も煩雑。**現実にはほぼ起きない**（完了した枠をわざわざ別日へ運ぶ）ため、
様子見。起こす価値が出たら `saveBox` / `save` の日付変更検知として足す。

---

### R14. 予定を1件だけ消した直後にタブを閉じると、その枠がクラウドに残る

**いまの状態**
2026-09-13 に `deleteCard()`（目標ごと消す）は直した。待ちを解いてから終わるようにしてある
（[報告](docs/reports/2026-09-13-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)）。

残っているのは `deleteTimeBox()`（予定シートから1件だけ消す）で、
`gc.timeboxes` の書き込みは 1.5 秒待つ側にある。消した直後にタブを閉じて
別端末で取り込むと、その枠だけが戻ることがある。**同じ端末で次に時間割を触れば
自動で片付く**ので優先度は低い。

素直に `flush` を足せないのは、カレンダー同期の一括削除
（`CalendarSyncBoot` の `for (const id of data.deletes) deleteTimeBox(id)`）で
**消した件数ぶん全件送信が走る**ため。直すならループの終わりで1回だけ送り切る形にする。

---

## 未検証のまま残っているもの

### R5. AI対話（`/story/new` → `/session`）の通し

`/api/chat` の課金が発生するため、自動検証では入口までしか踏んでいない。
`PHASE_INSTRUCTIONS` と `PHASE_TURN_LIMIT` のかみ合わせ（AGENTS.md に注意書きあり）は
実際に最後まで通さないと確認できない。

### R6. ログイン後のクラウド同期・カレンダー連携の本番往復

別端末で同じデータが見えるか、カレンダーの予定が本当に往復するか。
Googleアカウントへの権限付与とパスワードの入力を伴うので、代行できない。

**これに紐づく未確認が3つある。**

1. **設定画面の「読み取りの許可が足りません」ブロック**（2026-09-09 追加）。
   ログイン済みかつ連携済みでないと表示条件に入らないため、実画面に出せていない。
   2026-09-11 に理由を特定した。`CalendarLink` は**Supabaseにログインしていないと、
   カレンダーの状態を取りに行く手前で「先にログインしてください」に分岐する**ので、
   ログインを代行できない以上ここから先へ進めない。
   **時間割側の帯・端末の印・印が消えることまでは実機で確認済み**
   （応答を差し替えて3通りを通した。2026-09-11 の報告）
2. **`CalendarSyncBoot` の繋ぎ直し処理**。ログインしていないと
   `getSyncState()` が `off` のままで、そもそも走らない。
   ロジックは `tests/calendar-reconnect.test.mjs`（13件）で固定してある
3. **`google_calendar_links` のRLS**。実DBを触らない方針のため未確認

### R7. カレンダー連携が本番でまだ有効になっていない

[9/8の調査](docs/reports/2026-09-08-%E3%82%AB%E3%83%AC%E3%83%B3%E3%83%80%E3%83%BC%E6%9C%AA%E6%8E%A5%E7%B6%9A%E3%81%AE%E7%A2%BA%E8%AA%8D.md)のとおり、
本番には鍵（`GOOGLE_OAUTH_CLIENT_ID` / `_SECRET`）が入っていない。
**連携する手前で止まっている**ので、同期も重ね表示も試しようがない。

本人の操作が要る（秘密の値を入力できず、`vercel` CLIにもログインできない）。

```bash
vercel login
bash scripts/push-env-to-vercel.sh --apply
```

そのうえで、Google Cloud の承認済みリダイレクトURIに
`https://prototype-management-tool.vercel.app/api/calendar/callback` を追加し、
同意画面のスコープに `calendar.readonly` を足して、設定画面から連携する。

---

## 直したもの（記録。消してよい）

### 2026-09-13（目標削除の直後に閉じるとクラウドに孤児の枠が残る）

詳細は
[docs/reports/2026-09-13-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-13-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。
2026-09-11 のコードレビュー指摘1が、報告だけで修正が入っていなかったもの。

- ログイン中に目標を消し、**その1.5秒以内にタブを閉じる**と、クラウドに
  `card_id` が null の予定枠が残り、別端末で取り込むと
  「目標ごと消したはずの予定」が時間割に戻ってきていた。
  目標の削除（`gc.cards`）はすぐ届くのに、枠の削除（`gc.timeboxes`）は
  まとめ待ちに乗っていたため。クラウド側の外部キーは `timeboxes.card_id` を
  SET NULL にするだけで行を残す
- `setSyncFlushHook()` を足し、`deleteCard()` の末尾で待ちを解くようにした。
  消す操作は連打されないので、まとめる利点がそもそも無い
- 回帰テスト4件（`storage` 3件・`push-queue` 1件）。
  **修正前のコードでは3件が落ちること**を確認済み
- `npm test` / `npx tsc --noEmit` / `npm run build` 通過。
  ブラウザで目標の削除・その前後の画面を実際に操作して確認（コンソールエラー0件）。
  **クラウドへの実通信は未実施**（R6 と同じ理由）

**あわせて、2026-09-11 その2 の修正をブラウザで実際に操作して確認した。**
時刻付き習慣の枠を完了 → 取り消し → 再完了 → 削除まで通し、
「できた」の記録が付く／取り消される／枠と一緒に取り消されるところまで意図どおりだった。
前回「ブラウザ操作は未実施」と書いていたが、これで埋まった。

### 2026-09-11 その2（完了取り消しで習慣の記録が残る）

詳細は
[docs/reports/2026-09-11-次に取り組む改善3件と完了取り消しの修正.md](docs/reports/2026-09-11-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6%E3%81%A8%E5%AE%8C%E4%BA%86%E5%8F%96%E3%82%8A%E6%B6%88%E3%81%97%E3%81%AE%E4%BF%AE%E6%AD%A3.md)。

- 時刻を決めた習慣の枠を**完了 → 取り消し**しても、その日の「できた」記録が
  残り続け、連続日数と達成率が1日ぶん水増しされていた。時刻付き習慣は
  「今日の習慣」チェックリストに出ないので、後から取り消す手立ても無かった。
- 完了済みの習慣枠を**削除**したときも同じ（枠は消えるが記録は残る）。
- `clearHabitLogFromBox()` を追加し、`saveBox` / `save` / `deleteTimeBox` から
  「その枠がさっき付けた `done`（時刻が一致）」だけを取り消すようにした。
  手で付けた記録・`partial` / `skipped` / `missed` には触れない。
- 集計側（`habit.ts`）は無変更。記録が消えれば自動で「今日はまだ」に戻る。
- 回帰テスト7件（`tests/storage.test.mjs`、35 → 42 件）。`npm test` /
  `npx tsc --noEmit` / `npm run build` 通過。ブラウザ操作は 2026-09-13 に実施済み（下の 2026-09-13 の項を参照）。

### 2026-09-11（止まっていたタスクの棚卸しと修正）

詳細は
[docs/reports/2026-09-11-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-11-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。
どちらも 2026-09-10 のコードレビューが指摘したまま、修正が入っていなかったもの。

- **レビュー指摘1を直した。** 混んでいるだけの 403（`rateLimitExceeded` ほか）を
  「連携し直せば直る」と誤判定して、**再連携しても消えない案内**を端末に
  焼き付けていたのをやめた。逆に、再連携がいちばん要る `invalid_grant` は
  HTTP 400 で来るのでステータスだけでは拾えていなかった。
  `GoogleApiError` に本文の理由（`reason`）を持たせ、判定をそれに基づかせた
- **レビュー指摘2を直した。** 繋ぎ直しの後始末で、`localStorage` の容量超過に
  よって落とし切れなかった枠があっても「片付いた」目印を書いていた。
  目印が新しくなると二度とやり直されず、残った古いIDが処理窓に入った日に
  枠が黙って消える。**1件でも保存に失敗したら目印を書かない**ようにして、
  次回やり直せるようにした（`upsertTimeBox()` が保存の成否を返すようにした）

回帰テスト10件を追加し、**修正前のコードでは9件が落ちること**を確認済み。
ブラウザで実際に操作して、時間割の案内・枠の編集と保存・保存失敗の帯・
一覧／目標／わたしの各画面まで確かめた（コンソールエラー0件）。
**Googleへの実通信はしていない**（R6・R7 と同じ理由）。

### 2026-09-10（止まっていたタスクの修正）

詳細は
[docs/reports/2026-09-10-止まっていたタスクの修正.md](docs/reports/2026-09-10-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E4%BF%AE%E6%AD%A3.md)。

- **R8 を直した。** 1文字打つたびに時間割（と習慣）の全行をクラウドへ送っていたのを、
  打ち終わりを待って1回だけにした（`push-queue.ts`、キー別に1.5秒の末尾デバウンス）。
  localStorage への保存は今までどおり即座。閉じる・隠す瞬間に送り切る（`SyncBoot`）。
  ログアウト時と取り込み時は、待っているぶんを送らずに捨てる
- **R9 を直した。** 習慣の「どこでやるか」が、時間割の枠のメタ認知欄（対策）に
  `場所: …` として載るようにした（`habitBoxMeta()`）。実体化しても残る。
  場所の無い習慣の枠は今までどおり空
- 目標を消すときの確認文が、「やめた」習慣とその記録を数えていなかったのを直した
  （2026-09-09 の前日レビュー指摘1。報告だけで修正が入っていなかった）。
  `deleteImpactText()` に切り出し、やめた習慣の件数も言うようにした

ブラウザで実際に操作して確認済み（習慣の場所の表示・実体化・削除の確認文と実際の削除・
1文字ごとの localStorage 保存・空状態を含む周辺画面。コンソールエラーなし）。
**ログイン中のクラウド同期そのものは実通信で見ていない**（R6 と同じ理由）。

### 2026-09-09（その2：次に取り組む改善の調査）

詳細は
[docs/reports/2026-09-09-次に取り組む改善3件.md](docs/reports/2026-09-09-%E6%AC%A1%E3%81%AB%E5%8F%96%E3%82%8A%E7%B5%84%E3%82%80%E6%94%B9%E5%96%843%E4%BB%B6.md)。

- 対話で決めた「どこでやるか」が、明日の一歩の枠から消えていた
  （`firstStepMeta` に切り出し、If-Then と場所を両立させた。`goal-card.ts` / `card/page.tsx`）
- 過去の提案3件（不正UUID・初回オンボーディング・振り返りの再表示）は
  すべて実装済みであることを確認。再提案しない
- 判断待ちを2件足した（上の R8 / R9）

### 2026-09-09

詳細は
[docs/reports/2026-09-09-止まっていたタスクの棚卸しと修正.md](docs/reports/2026-09-09-%E6%AD%A2%E3%81%BE%E3%81%A3%E3%81%A6%E3%81%84%E3%81%9F%E3%82%BF%E3%82%B9%E3%82%AF%E3%81%AE%E6%A3%9A%E5%8D%B8%E3%81%97%E3%81%A8%E4%BF%AE%E6%AD%A3.md)。

- 繋ぎ直しのあと、時間割の枠が時間差で黙って消える（古いIDを localStorage 本体から落とす）
- 一度も同期に成功していない端末で、繋ぎ直しを永久に検知できない
- 重ね表示の403（権限不足）が握りつぶされて、理由がどこにも出ない
- `refresh_token` の重みが変わったのに、`link.ts` / `google.ts` の前提が古いまま
- `/api/calendar/overlay` だけ zod を通していない
- 習慣の作成日をUTC文字列から切り出していて、作った時刻で日数がぶれる
- 日本語として言わない「はじめて0日目」

**R4（ドラッグの実機確認）は今回ブラウザで実際に操作して確認したので、この項目は閉じた。**
移動・上端／下端のリサイズ・長押しの成立と不成立・つまみ・引いて作る、
マウスと指の両方で意図どおりだった。
ただしブラウザ自身のヒットテストとジェスチャ調停は経由していないので、
**指で1回だけ触ってもらえると完全に埋まる**。

### 2026-09-08

詳細は
[docs/reports/2026-09-08-目標と時間割のつながりを直す.md](docs/reports/2026-09-08-%E7%9B%AE%E6%A8%99%E3%81%A8%E6%99%82%E9%96%93%E5%89%B2%E3%81%AE%E3%81%A4%E3%81%AA%E3%81%8C%E3%82%8A%E3%82%92%E7%9B%B4%E3%81%99.md)。

- 目標を消すときに、巻き添えになる予定・習慣の件数を出す
- 完了した目標の習慣を、時間割と今日のチェックリストから外す
- 「手入力でつくる」「＋ 繰り返すことを足す」で、押しただけでは保存しない
- 予定シート・打刻バーで、完了した目標に紐づいた予定が「（紐づけない）」に見える食い違い
- 目標から「時間割で予定を作る →」に、目標を引き継ぐ
- 今日のリストが時刻順に並ばない
- ログインしていないとGoogleカレンダー連携ボタンが押せてしまう
- 表記のズレ（`何をを編集` / `M1〜M7`）
